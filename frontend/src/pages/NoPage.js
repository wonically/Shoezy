import { Typography } from "@mui/material";
import React from "react";

const NoPage = () => {
  return (
    <Typography>
      There is no content on this page. Try accessing from homepage again.
    </Typography>
  );
};

export default NoPage;
